# A4 ACCR-MPC implementation

The ordinary controller uses only causal public measurements. A finite active certificate may enlarge the performance envelope, while the hard contract floor remains unchanged. Every control call solves the registered two-branch rolling QP with full state/input/SoC/delay/reserve sequences. The current action is shared by delivered and surplus-loss branches; future SG and reserve decisions provide recourse. Issued actions are exactly decomposed into contract, certified and allocation-neutral probe components, and only the action actually applied is committed. Solver attempts, restoration and fallback are counted separately.

Focused full-nonlinear Gate: **PASS**.
