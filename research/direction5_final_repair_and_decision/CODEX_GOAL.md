# Codex唯一总Goal：方向5最终修复与裁决

## 0. 命名
```text
方向5 / DIRECTION5 / direction5
```

## 1. 阅读
完整阅读：

```text
research/direction5_final_repair_and_decision/
```

下全部文件。

## 2. 连续执行
严格执行：

```text
R0 → R1 → R2 → R3 → R4 → R5 → R6 → R7 → R8
```

不得在内部阶段后等待用户再次发消息。

## 3. 唯一方法
```text
DCSV-CR-MPC
Disturbance–Capability-Separated Contract-Recourse MPC
```

不允许临时添加AI/RL或更换核心科学问题。

## 4. 必须修复

1. 撤回Phase I的决定性终止结论，重算正确统计；
2. 禁止使用mean episode-relative ratio作为主指标；
3. 使用scenario-balanced aggregate means和seed/design-cell hierarchical bootstrap；
4. solver分母包括所有attempted optimization calls；
5. 加入contract-only rolling MPC作为首要公平基线；
6. 重建真正set-membership/MHE deliverability set；
7. 区分contract floor与online performance envelope；
8. 实现contract-safe + surplus-loss recourse场景树；
9. 能量使用真实SoC状态；
10. 连续delay有外包/稠密验证；
11. 每个核心episode包含未通知能力变化和完整滚动控制；
12. Plant A完整非线性，Plant B原生ANDES；
13. normal1h真实仿真并设频率品质Gate；
14. 所有MPC基线必须真实滚动优化；
15. final前完成理论与证书；
16. 不删除失败、不降低标准、不修改final结果；
17. R5失败后必须终止，不得创建新Phase。

## 5. 唯一终态

```text
PAPER_READY_WITH_BOUNDED_CLAIMS
```

或：

```text
DIRECTION5_TERMINATED_WITH_DECISIVE_NEGATIVE_EVIDENCE
```

## 6. 最终ZIP

```text
DIRECTION5_FINAL_REPAIR_AND_DECISION_SINGLE_REVIEW_PACKAGE.zip
```

小于512MB，严格符合：

```text
research/direction5_final_repair_and_decision/
10_FINAL_REVIEW_PACKAGE_SPEC.md
```

完成后报告：
- ZIP路径、大小、SHA256；
-Git commit/status；
-R0–R8；
-材料性；
-selected observer/estimator；
-best baseline；
-known/OOD/contract violation；
-normal1h；
-solver/restoration/fallback；
-theory certificate；
-Plant A/B；
-最严重失败；
-唯一最终状态。
