项目命名锁定：本项目统一称为“方向5 / DIRECTION5 / direction5”。

请先完整阅读当前仓库：

research/direction5_phase_i_final_convergence/

下的全部文件，然后将 CODEX_GOAL.md 作为本项目唯一总Goal立即执行。

请按 I0→I1→I2→I3→I4→I5→I6→I7→I8 连续完成，
不要在内部阶段后等待我再次发消息。

本轮是方向5的最终科学收敛，不允许继续无限创建新阶段。

必须：

1. 冻结并纠正Phase H，撤回其H7作为方法证据；
2. 修复seed因素混杂、人工normal1h零行、短时控制后held tail、
   reduced Plant B surrogate和无unannounced capability transition；
3. 使用完整非线性Plant A和原生ANDES Plant B；
4. 每个核心episode包含nominal warm-up、随机能力变化和完整滚动控制；
5. 隐藏能力范围收缩为power/ramp/delay；
   energy由测得SoC计算，availability折算进deliverability；
6. 区分contract guaranteed floor与online performance envelope；
7. 负荷观测器使用actual BESS POI power；
8. 用因果set-membership/MHE维护deliverability set；
9. 实现完整滚动DCSV-MPC，包括真实SoC、delay pipeline、
   slow reserve、sustainable/bridge/infeasible和feasibility restoration；
10. 所有MPC基线都必须是真实滚动优化；
11. 完成不可保证边界和条件性理论；
12. development/validation/final严格隔离；
13. final锁定后禁止调参；
14. 不得删除失败、降低物理标准、伪造正常运行结果，
    或用reduced surrogate冒充native Plant B；
15. 若I6在两轮有依据修复后仍失败，必须输出决定性负结果并停止，
    不得再创建Phase J/K或临时更换算法。

最终只允许输出：

PAPER_READY_WITH_BOUNDED_CLAIMS

或

DIRECTION5_TERMINATED_WITH_DECISIVE_NEGATIVE_EVIDENCE。

最终生成小于512MB的：

DIRECTION5_PHASE_I_FINAL_CONVERGENCE_SINGLE_REVIEW_PACKAGE.zip

内容严格符合：

research/direction5_phase_i_final_convergence/
09_FINAL_REVIEW_PACKAGE_SPEC.md

完成后报告ZIP路径、大小、SHA256、Git commit、全部Gate、
H1–H6、selected observer/estimator、最佳基线、Plant A/B、
known/OOD、normal1h、solver/fallback、certificate和最严重限制。
