# 方向5 Phase I：最终科学收敛与投稿级完成

## 项目命名锁定

本项目永久统一命名为：

```text
中文：方向5
英文标识：DIRECTION5
代码/目录标识：direction5
```

历史文件中的 `DIRECTION1`、`ST-BOUND` 等名称只能作为旧证据引用。

## 当前阶段定位

本启动包用于审查并继续：

```text
DIRECTION5_PHASE_H_DCSV_MPC_SINGLE_REVIEW_PACKAGE.zip
```

Phase H 不是一个可投稿结果。它的高层问题仍然成立，但 H7 方法结论受到实验设计、能力集合语义、Plant B替代、闭环执行时域和统计样本等结构性问题影响。

本轮不是再做一个短阶段，也不允许继续反复换算法。唯一目标是：

> 在修正后的物理、信息与统计框架下，把方向5收敛为一篇可投稿的方法论文，或者形成具有决定性证据的终止报告。

唯一方法名称继续使用：

> **DCSV-MPC：Disturbance–Capability-Separated Viability MPC**

但其科学范围和实现必须按本包重建，不能把 Phase H 的启发式能力估计器和缩短闭环实验继续作为正式方法。

## 目录放置

解压到当前真实 Git 仓库：

```text
research/direction5_phase_i_final_convergence/
```

然后把 `CODEX_GOAL.md` 作为唯一总 Goal 发送给 Codex。
