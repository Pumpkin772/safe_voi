项目命名锁定：方向5 / DIRECTION5 / direction5。

请先完整阅读当前仓库：

research/direction5_accr_mpc_one_goal/

下的全部文件，并将 CODEX_GOAL.md 作为唯一总Goal执行。

请连续完成 A0→A1→A2→A3→A4→A5→A6→A7→A8，
不要在阶段间等待新的消息。

唯一方法：

ACCR-MPC：
Active Capability Certification and Recourse MPC。

必须：

1. 冻结历史DCSV-CR负结果，不覆盖；
2. 修复normal1h平台并重验perfect information materiality；
3. 实现完整候选模型集合和被动基线；
4. 设计SG–IBR分配中性的事件触发主动探测；
5. 探测必须在所有候选和不交付分支下满足frequency/ACE/tie和物理约束；
6. 用探测数据形成有限有效期的power/ramp/delay能力证书；
7. 实现合同、认证、探测分量和能力丢失追索；
8. 使用新的development/validation/final seeds；
9. development/validation内最多三轮预注册修复，final后禁止调参；
10. 不得删除失败、降低标准、扩大事故制造结果或临时更换算法；
11. 使用论文初稿模板，用真实结果替换所有预测；
12. A6失败后正式终止方向5，不再创建新Phase。

最终只允许：

PAPER_READY_WITH_BOUNDED_CLAIMS

或：

DIRECTION5_TERMINATED_WITH_DECISIVE_NEGATIVE_EVIDENCE。

最终生成小于512MB的：

DIRECTION5_ACCR_MPC_SINGLE_REVIEW_PACKAGE.zip

内容严格符合：

research/direction5_accr_mpc_one_goal/
09_FINAL_REVIEW_PACKAGE_SPEC.md

完成后报告ZIP路径、大小、SHA256、Git commit、全部Gate、
H1–H6、selected probe policy、能力集合收缩、probe safety/cost、
value recovery、最佳基线、Plant A/B、known/OOD、normal1h、
solver/fallback、certificate、最严重失败和唯一最终状态。
