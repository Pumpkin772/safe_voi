项目命名锁定：本项目统一称为“方向5 / DIRECTION5 / direction5”。
所有新建目录、Git分支、结果、图表、报告和最终ZIP必须使用该命名；
历史材料中的其他命名仅作为旧证据引用，不得继续沿用。

请先完整阅读当前仓库：

research/direction5_phase_h_dcsv_mpc/

下的全部文件，然后将 CODEX_GOAL.md 作为本项目唯一总Goal立即执行。

请按 H0→H1→H2→H3→H4→H5→H6→H7→H8→H9 连续完成，
不要在内部阶段后等待我再次发消息。

本轮保持方向5科学问题不变，唯一方法为：

DCSV-MPC：
Disturbance–Capability-Separated Viability MPC。

关键要求：
1. 将Phase G G2负结论重分类为终端样本与不确定性模型错误；
2. 审计near-terminal窗口中的饱和、GRC、边界和不可持续状态；
3. 先完成sustainable/bridge/infeasible分类和负荷依赖平衡点；
4. 电网负荷估计使用actual BESS POI power作为已知输入；
5. command-to-power能力集合单独估计；
6. persistent load error不得每周期重复为新事故；
7. 只用物理干净、可持续、无事件、无fallback窗口校准terminal set；
8. coverage报告有限样本置信下界；
9. 实现真正滚动DCSV-MPC和真实MPC基线；
10. ordinary controller禁止读取true capability、true load和future event；
11. final锁定后禁止调参，不删除失败或降低标准；
12. 下一包必须包含完整可运行source snapshot。

最终生成小于512MB的：

DIRECTION5_PHASE_H_DCSV_MPC_SINGLE_REVIEW_PACKAGE.zip

内容严格符合：

research/direction5_phase_h_dcsv_mpc/10_FINAL_REVIEW_PACKAGE_SPEC.md

完成后报告ZIP路径、大小、SHA256、Git commit、全部Gate、H1–H6、
selected observer/estimator、sustainable/bridge/infeasible统计、
最佳基线、known/OOD结果、solver/fallback、certificate和最严重限制。
