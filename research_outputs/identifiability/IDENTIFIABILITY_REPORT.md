# Passive identifiability report

Validation uses only external issued-command and measured-power histories. `P(Tdet<Tcrit)=1.000`, false alarm `0.000`, and source macro-F1 `1.000`. Per-mechanism timing probabilities and every non-detection remain in the episode table. Final seeds were not used.
